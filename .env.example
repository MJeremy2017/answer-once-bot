# Lark app credentials (from Developer Console)
# Use quotes if the value contains special characters (e.g. LARK_APP_SECRET="your-secret")
LARK_APP_ID=
LARK_APP_SECRET=

# Lark API base URL: https://open.larksuite.com (Lark) or https://open.feishu.cn (Feishu)
LARK_BASE_URL=https://open.larksuite.com

# Optional: bot's open_id. Set when using "all group messages" permission so the bot only answers when @mentioned.
# Leave empty if you only have "Obtain group messages mentioning the bot" (then every received message is considered a mention).
# LARK_BOT_OPEN_ID=

# Optional: comma-separated chat IDs to limit indexing/matching to these channels
# ANSWERED_ONCE_CHAT_IDS=oc_xxx,oc_yyy

# Similarity threshold (0.0-1.0). Higher = stricter match.
SIMILARITY_THRESHOLD=0.78

# Answer mode: top_1 (single best match) or llm_summarize (top-k + LLM summary)
# ANSWER_MODE=top_1

# Max similar Q&As to fetch (for llm_summarize)
# TOP_K_CANDIDATES=5

# For top_1: which candidate to pick. similarity | recency | longest
# BEST_ANSWER_POLICY=similarity

# LLM (required when ANSWER_MODE=llm_summarize)
# OPENAI_API_KEY=sk-...
# LLM_MODEL=gpt-4o-mini
# LLM_BASE_URL=

# Path for Chroma DB persistence (default: ./data/chroma)
# CHROMA_PERSIST_DIR=./data/chroma
